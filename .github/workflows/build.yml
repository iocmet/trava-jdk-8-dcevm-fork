name: Go

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    name: Build
    runs-on: windows-2019
    steps:
    - name: Run windows build
      shell: pwsh
      run: ./build_windows.ps1
